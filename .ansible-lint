use_default_rules: true
parsable: true

exclude_paths:
  - venv/
  - molecule/default/
  - tests/

skip_list:
  - '502'  # don't alert if an unnamed task is encountered
  - '301' # don't alert on usage of command module
